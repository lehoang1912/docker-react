language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t lnhoang2769/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true lnhoang2769/docker-react npm run test